# @package _global_
# to execute this experiment run:
# python train.py experiment=example
defaults:
  - override /data: local/segmentation_basement_membrane_from_czi.yaml
  - override /model: local/segmentation_basement_membrane.yaml
  - override /callbacks: default.yaml
  - override /trainer: gpu.yaml
  - override /logger: mlflow.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["dev"]
seed: 12345
# basement_membrane_semseg_version_6_early_model_bigrun_H2B_EOMES
experiment_name: basement_membrane_semseg
run_name: basement_membrane_semseg
source_col: path
target_col: out_path
column_channel: channel
scene_col: scene
spatial_dims: 3
raw_im_channels: 1

paths:
  output_dir: /allen/aics/assay-dev/computational/data/EMT_deliverable_processing/Collagen_segmentation_segmentations/membrane_thickness_proof_of_concept

checkpoint:
  ckpt_path: /allen/aics/assay-dev/computational/data/EMT_deliverable_processing/cytodl_experiments/logs/train/runs/basement_membrane_semseg/basement_membrane_semseg_version_6_early_model/2023-12-15_16-09-07/checkpoints/epoch_478.ckpt

data:
  _aux:
    patch_shape: [8, 512, 512]

